development:
  driver: postgres
  dsn: postgres://localhost/syracuse?sslmode=disable

test:
  driver: postgres
  dsn: postgres://localhost/syracuse_test?sslmode=disable

docker:
  driver: postgres
  dsn: postgres://gotoschool:goto1234@postgres:5432/greek?sslmode=disable

kubernetes:
  driver: postgres
  dsn: postgres://gotoschool:goto1234@localhost:5432/greek?sslmode=disable